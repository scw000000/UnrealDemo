// Fill out your copyright notice in the Description page of Project Settings.

#include "Demo.h"
#include "BasicAIController.h"
/*
void BeginPlay( const FObjectInitializer& ObjectInitializer ) : Super( ObjectInitializer
   {
   // Set this character to call Tick() every frame.  You can turn this off to improve performance if you don't need it.
   PrimaryActorTick.bCanEverTick = true;
   //floating point var initialization
   Speed = 20.f;
   HitPoints = 20.f;
   BaseAttackDamage = 0.f;
   AttackTimeout = 0.f;
   //-1: attackable, >= 0: cooling down
   TimeSinceLastStrike = -1.f;
   //int32 var initialization
   Experience = 0.f;

   BPLoot = NULL;

   SightSphere = ObjectInitializer.CreateDefaultSubobject<USphereComponent>( this, TEXT( "Sight Sphere" ) );
   SightSphere->AttachTo( RootComponent );

   AttackRangeSphere = ObjectInitializer.CreateDefaultSubobject<USphereComponent>( this, TEXT( "Attack Range Sphere" ) );
   AttackRangeSphere->AttachTo( RootComponent );
   }

ABasicPlayerController( const FObjectInitializer& ObjectInitializer )
{
   PrimaryActorTick.bCanEverTick = true;
}

void ABasicAIController::Tick( float DeltaTime )
   {
   Super::Tick( DeltaTime );
   }

*/